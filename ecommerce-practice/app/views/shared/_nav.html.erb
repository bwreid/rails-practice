<nav class="top-bar">
  <ul class="title-area">
    <!-- Title Area -->
    <li class="name">
      <h1><%= link_to("ecommerce project practice", root_path) %></h1>
    </li>
    <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">

    <!-- Right Nav Section -->
    <ul class="left">
      <% if @auth && @auth.is_admin %>
        <li><%= link_to("New Product", new_product_path, :remote => true ) %></li>
      <% end %>
    </ul>

    <ul class="right">
      <% if @auth %>
        <li><%= link_to("Logout", login_path, :method => 'delete', :remote => true) %></li>
      <% else %>
        <li><%= link_to("Login", login_path, :remote => true ) %></li>
      <% end %>
    </ul>
  </section>
</nav>