.row
  .large-12.columns
    %h1 FLIRT
    - if @auth.nil?
      .sign-in-links
        = link_to( 'Register', new_subscriber_path, :remote => true, :class => 'button' )
        = link_to( 'Login', login_path, :remote => true, :class => 'button' )
    #new