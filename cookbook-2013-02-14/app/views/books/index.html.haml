- @books.each do |book|
  %ul{ :class => 'polaroids' }
    %li
      = link_to(image_tag(book.image), book, :title => book.name)
  %p
    = book.cuisine
    cuisine. This book includes:
  %ul This book includes:
  - book.recipes.each do |recipe|
    %li= link_to(recipe.name, recipe)
    / = link_to(image_tag(recipe.image), recipe)
  %div{ :style => 'clear: both' }