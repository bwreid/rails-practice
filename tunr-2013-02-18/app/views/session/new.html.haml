%h3 Login
%p
  Please enter your account information below. Don't have an account?
  %span{ :style => 'font-weight: bold;' }= link_to("Register for an account now!", new_user_path)

- if session[:user_id].blank?
  = form_tag '/login' do
    .row
      %fieldset
        %legend Login to Tunr
        %br
          .six.columns
            = label_tag 'username'
            = text_field_tag 'username', nil, :autofocus => true
          .six.columns
            = label_tag 'password'
            = password_field_tag 'password'
      = submit_tag 'Login to Tunr', :class => 'button'
- else
  %p You've already logged in!