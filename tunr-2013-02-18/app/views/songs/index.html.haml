%h3 Songs

.row
  - @songs.each do |song|
    .new-row
      .four.columns
        .row-crop
          - song.am_i_empty
          = link_to( image_tag(song.artist ? song.artist.image : 'http://placehold.it/400x300' ), song)
        %p= link_to(song.name, song)